/*#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>

int main() {
	int num[5], pas = 0;

	for (int i = 0; i < 5; i++){
		scanf("%d", &num[i]);
		pas += pow(num[i], 2);
	}

	printf("%d", pas % 10);

	return 0;
}*/